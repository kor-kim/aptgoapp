<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" 
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AptgoVehicleManager"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        
        <!-- Login Activity -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Main Activity -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.AptgoVehicleManager.NoActionBar" />
        
        <!-- Camera Scan Activity -->
        <activity
            android:name=".activities.CameraScanActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        
        <!-- Manual Search Activity -->
        <activity
            android:name=".activities.ManualSearchActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        
        <!-- Vehicle Detail Activity -->
        <activity
            android:name=".activities.VehicleDetailActivity"
            android:exported="false" />
        
        <!-- Vehicle Data View Activity -->
        <activity
            android:name=".activities.VehicleDataViewActivity"
            android:exported="false"
            android:theme="@style/Theme.AptgoVehicleManager.NoActionBar" />
        
        <!-- History Activity -->
        <activity
            android:name=".activities.HistoryActivity"
            android:exported="false" />
        
        <!-- Report Activity -->
        <activity
            android:name=".activities.ReportActivity"
            android:exported="false" />
        
        <!-- Settings Activity -->
        <activity
            android:name=".activities.SettingsActivity"
            android:exported="false" />
            
    </application>

</manifest>